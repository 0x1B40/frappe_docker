# Production Environment Variables for Frappe/ERPNext
# Copy this file and customize for your production environment

# Database Configuration
DB_HOST=mariadb
DB_PORT=3306
MYSQL_ROOT_PASSWORD=admin

# Redis Configuration
REDIS_CACHE=redis-cache:6379
REDIS_QUEUE=redis-queue:6379

# Frappe Configuration
FRAPPE_SITE_NAME_HEADER=$host

# Custom Image (leave empty for default frappe/erpnext images)
CUSTOM_IMAGE=
CUSTOM_TAG=v15.85.1

# Restart Policy
RESTART_POLICY=unless-stopped

# Pull Policy
PULL_POLICY=always

# Proxy Configuration (for production with reverse proxy)
UPSTREAM_REAL_IP_ADDRESS=127.0.0.1
UPSTREAM_REAL_IP_HEADER=X-Forwarded-For
UPSTREAM_REAL_IP_RECURSIVE=off
PROXY_READ_TIMEOUT=120
CLIENT_MAX_BODY_SIZE=50m

# Socket.IO Port (internal)
SOCKETIO_PORT=9000
